alias ll="ls -al"
alias ln="ln -v"
alias m="rails db:migrate db:rollback && rails db:migrate db:test:prepare"
alias mkdir="mkdir -p"
alias path='echo $PATH | tr -s ":" "\n"' # Pretty print the path

if (( $+commands[nvim] )) ; then
  alias vim="nvim"
fi
