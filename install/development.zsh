header "Setting up development environment"

# install and activate programming languages with latest versions
mise use -g node # Installs ~/.default-npm-packages on install (not activate)
mise use -g python # Installs ~/.default-python-packages on install (not activate)
mise use -g ruby # Installs ~/.default-gems on install (not activate)